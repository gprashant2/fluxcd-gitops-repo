apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: my-nginx
  namespace: default
spec:
  interval: 1m
  chart:
    spec:
      chart: ./charts/my-nginx
      sourceRef:
        kind: GitRepository
        name: fluxcd-gitops-repo
        namespace: flux-system
